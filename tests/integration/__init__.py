"""Live integration suite.

Everything here talks to a real SkyLink API deployment and is therefore gated
behind the ``SKYLINK_TEST_BASE_URL`` environment variable — see
:mod:`tests.integration.conftest`.
"""
